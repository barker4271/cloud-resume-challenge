<h1>Projects</h1>

<section class="projects">
  <% projects.forEach(project => { %>
    <article>
      <h2><%= project.name %></h2>
      <p><%= project.description %></p>
    </article>
  <% }) %>
</section>
